# dax-main workspace .gitignore
# Only frontend-specific ignores (root .gitignore handles the rest)

# React-specific cache files (if any beyond what root handles)
.eslintcache

# Frontend-specific environment files (if any beyond what root handles)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Frontend-specific temporary files
src/temp/

