#//dax-main/.gitignore
# ─── Dependencies ────────────────────────────────────
/node_modules/               # root dependencies
/dax-main/node_modules/      # if you ever add a node_modules under dax-main

# ─── Build & Production ─────────────────────────────
/build/                      # root build output (if you ever run `npm run build` here)
/dax-main/build/             # CRA build output from dax-main

# ─── Environment & Secrets ──────────────────────────
.env                         # local env vars
.env.*.local                 # local override files
!.env.example                # but keep your example

# ─── Firebase emulator data ─────────────────────────
/.firebase/                   # local emulator state

# ─── Logs ───────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ─── Caches & Coverage ──────────────────────────────
.cache/
.tmp/
coverage/
jest-coverage/
dist/
.out/

# ─── IDE & OS files ─────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.DS_Store
Thumbs.db

# ─── Keys & Credentials ─────────────────────────────
keys/
*.key
*.pem
credentials/*.json